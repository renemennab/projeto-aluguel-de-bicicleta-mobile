import styled from 'styled-components'

export const StyledButton = styled.button`
    border: none;
    border-radius: 100px;
    padding: 15px 32px;
    background: var(--dark-blue);
    color: white;
`
